package groovy.learning

/**
 * This class shows how to create and use Set in Groovy
 *
 */
class GroovySet {

	static main(args) {
		def mySet = ['Deer', 'Elephant'] as Set
		mySet.add 'Tiger'
		mySet += 'Monkey'
		mySet << 'Snake'

		mySet.each { i -> println i }
	}
}
